# CapOwn Master — Nginx configuration snippet
# SPDX-License-Identifier: AGPL-3.0-only

# Merge into your HTTPS server block in /etc/nginx/sites-available/<your-domain>

location /gb/ {
    proxy_pass http://127.0.0.1:9210/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # SSE requires disabled buffering
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;

    # Support Upgrade header (future WebSocket use)
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
